function SECRETO(cad){
    return cad.split("").reverse().join(""); 
    //Separa la cadena con split(""). La invierte por separado con reverse(). Y une todos los elementos de la cadena separada e invertida con join("")
}
 
